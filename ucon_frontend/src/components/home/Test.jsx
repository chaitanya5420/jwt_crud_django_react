import React from "react";
import { Typography, Box } from "@mui/material";

const Test = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" sx={{ color: "#ffffff" }}>This is Home</Typography>
    </Box>
  );
};

export default Test;
